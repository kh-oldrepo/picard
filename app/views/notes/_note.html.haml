.note{:data => {:id => note.id}}


  .text= note.text
  .edit= link_to 'edit', edit_note_path(note.id), :remote => true
  = link_to 'Destroy', note, method: :delete, data: { confirm: 'Are you sure?' }