.note{:data => {:id => note.id}}


  .text= note.text


  %tr
    %td.edit= link_to 'Edit', edit_note_path(note.id), :remote => true
    \|
    %td= link_to 'Destroy', note, method: :delete, data: { confirm: 'Are you sure?' }