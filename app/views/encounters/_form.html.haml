= simple_form_for(@encounter) do |f|

  .form-inputs
    = f.input :name
    = f.input :tag_list, :as => :text,:input_html => { :rows => 3, :class => "text-box"}


  .form-actions
    = f.button :submit


